# 导师调研工具 需求文档

状态：v0.6（2026-09-05）
文档分工：本文管需求/范围/决策；`ARCHITECTURE.md` 管架构与数据模型；`HANDOVER.md` 管现状/踩坑/操作手册。

一期定位：全量导师库。套磁信、学术画像（论文/引用）、AI 归纳整体延后，设计压缩在附录 A 保留；数据源先只考虑院系官网；规模为符合口径的全量（千级），采用名单驱动、渐进富化。

---

## 1. 目标

一句话：**把目标院校相关院系官网的师资信息，抓成一张全量、可浏览、可筛选、可追溯的导师表。**

一期只回答三个问题：

- 某学校 / 院系 / 职称 / 方向下有哪些导师（全集，不是精选）
- 这位导师的基础信息是什么，每条信息出处是哪
- 哪些信息缺失，该去哪补

反判据，出现任一即视为做错方向：

- 官网上有的信息库里没有，且没有失败留痕——静默丢数据
- 无出处的字段和爬来的字段长得一样
- 为了填满字段而编造或脑补值

---

## 2. 范围

### 2.1 学校与单位

学校清单配置在 `data/schools.yaml`，分档如下：

| 档 | 说明 | 学校 |
| ---- | ---- | ---- |
| T1 | 清北 + 华五 | 清华、北大、复旦、上交、浙大、中科大、南大 |
| T2 | CS 强势 985 | 北航、哈工大、西交、华科、东南、北理工、电子科大、人大、南开、天大、武大、中山、厦大、同济、山大、中南、华南理工、西工大、华东师大 |
| T2b | 其余 985，CS 中等，作保底看 | 吉大、大连理工、东北大学、湖南大学、重庆大学、四川大学、北师大 |
| T3 | 顶尖 211 与特殊院校，本期冻结不再加校 | 北邮、西电、国科大、国防科大、南科大、上科大 |
| 排除 | 政策性入选且 CS 弱 | 西农、中农、中海洋、中央民族、兰大 |

单位粒度到院系，不是学校。同一所学校的计算机学院、软件学院、人工智能学院要分开，招生名额和风格完全不同。试点为上交，目标是其 CS 相关院系全量入库。

### 2.2 专业口径

**CS 相关院系全要**：计算机学院、软件学院、人工智能学院、网络空间安全学院等 CS 直接相关院系全部纳入，不做方向预筛。院系清单逐校圈定，写进该校的抓取配置。电院/信息学院等大院内非 CS 本体的系（电子、微电子等）默认不进，要加改一行配置即可。

### 2.3 明确不做

- 不爬知乎、一亩三分地等口碑内容
- 不抓导师个人主页正文——官网上的外链只存 URL 不跟进，个人主页内容属延后项
- 不做信件、不做群发
- AI 不在本期关键路径上

---

## 3. 名单驱动的数据流

这是本次重构的核心形态变化。流程分三步，每步独立可验收：

| 步骤 | 动作 | 产出 |
| ---- | ---- | ---- |
| 1. 建名单 | 抓师资列表页（含翻页），提取姓名、职称、详情页链接 | 全量 roster 落库，立即可浏览 |
| 2. 富化 | 逐个抓详情页，按字段映射解析 | 字段补齐，逐字段带出处 |
| 3. 留痕 | 解析失败、字段缺失、官网缺失 | 显式记录进 issues，不静默 |

每位导师一条生命周期状态：`roster`（仅名单）→ `enriched`（详情已解析）→ `verified`（人工核对过）。

**覆盖率是第一验收指标**：名单总数、已解析数、失败数三个数字必须能对上官网实际（官网列表页本身有陈旧条目的，差异留痕而不是硬凑相等）。

---

## 4. 数据模型

### 4.1 文件布局

| 路径 | 内容 | 手改 |
| ---- | ---- | ---- |
| `data/schools.yaml` | 学校清单与档位 | 是 |
| `data/professors/<school>/<dept>/<slug>.yaml` | 一位导师一个文件 | 是 |
| `data/issues/<school>-<dept>.yaml` | 该院系的解析失败与待人工队列 | 否（工具生成，人工销账） |
| `sites/<school>/<dept>.yaml` | 该院系的爬取配置 | 是 |
| `cache/http/` | 原始 HTML 响应 | 否 |
| `data/student.yaml` | 占位，随延后项启用 | 是 |

### 4.2 key 与重名

不能用 `<school>/<pinyin>.yaml` 做唯一键：拼音在千级全量下重名必现（张伟、王磊这类）。改为：

- `slug` = 拼音；同院系撞名加序号（`zhangwei`、`zhangwei-2`）
- 文件内 `aliases` 存曾用名/英文名
- 同一人在同校多个院系任职：`primary_dept` + `joint_appointments` 列表，文件归 primary_dept

### 4.3 provenance

不给每个标量套壳，按字段路径索引 provenance 块：

```yaml
admission:
  quota: 3
provenance:
  admission.quota:
    origin: crawled          # crawled | computed | ai | manual
    source: https://example.edu.cn/notice/2026.html
    fetched_at: 2026-09-02
    confidence: auto         # verified | auto | unverified
```

- `crawled` 直接抓到的原始事实；`computed` 由抓到的数据确定性算出（本期基本用不到）；`ai` 模型归纳，必带 `model`、`prompt_version`、`evidence`（延后项）；`manual` 人工填写
- `confidence` 三档：`verified` 人工核对过、`auto` 程序抓来未核对、`unverified` 传闻或推测

本期实际只会用到 `crawled` + `manual`。

### 4.4 字段与官网可得性

| 字段 | 官网可得性 | 获取 |
| ---- | ---- | ---- |
| 姓名、职称、详情页链接 | 必有 | 师资列表页 |
| 院系归属 | 必有 | 由抓取配置给定 |
| **邮箱** | 常有 | 详情页。**除姓名外最重要的字段（用户明确要求）**：低覆盖院系必须抽查页面验证是解析问题还是真缺失，不得凭解析结果断言"官网没有" |
| 办公电话 | 常有 | 详情页 |
| 研究方向（官网自述文字） | 常有 | 详情页，**原样保存**，不做归一化（归一化属延后的 AI 项） |
| 个人主页 URL | 常有 | 详情页，只存不抓 |
| 博导/硕导标识 | 常有（栏目或标签形式） | 列表页或详情页 |
| 教育背景、工作经历 | 约半数 | 详情页 |
| 导师级招生信息（名额、是否招满） | 罕见 | 官网基本没有，工具的责任是给院系简章直达链接 + 生成待确认清单 |
| 院系级招生信息（统考/推免/简章） | 常有 | 招生通知页，存为院系级记录，挂在该院系所有导师下 |
| 论文、引用、代表作、合作者 | **本期为空** | 随 DBLP/OpenAlex 延后 |

原则：官网没写的字段就是没有，值留空 + 页面显示"官网未提供"，不用推测填充。

---

## 5. 爬取框架

每院系一个适配器，配置与代码分离：

- `sites/<school>/<dept>.yaml`：列表页 URL、翻页规则、列表条目选择器、详情页各字段映射
- 复杂站点允许配内嵌 XPath/正则；规则实在表达不了的，写独立 python 钩子模块（预期少数）
- 编码处理：老站常见 GBK/GB2312，强制按响应头 + 内容嗅探双通道处理
- 限速 + 重试；原始 HTML 进 `cache/http/`，重跑不发新请求
- 官网改版 → 选择器失效 → 解析失败进 issues 队列并报出，不允许静默产出空字段

已知风险：存在 JS 渲染的院系站，纯 requests 拿不到内容。本期先不引入浏览器引擎，遇到就记 issues，是否引入 Playwright 列为未决项。

---

## 6. 交付形态

数据层 Python（`requests` / `pyyaml` / `jinja2` 够用）。前端用 Jinja2 生成静态 HTML + 原生 JS：零构建、生成物进 git、双击就能开。千级数据量的筛选排序是纯客户端逻辑，可承受；未来真要做拖拽看板再评估前端框架。

页面（本期只有前三个）：

| 页面 | 内容 | 期 |
| ---- | ---- | ---- |
| 导师列表 | 全量表格，按学校/院系/职称/研究方向关键词/博导标识筛选，支持排序，显示生命周期状态 | P2 |
| 导师详情 | 全字段 + 逐字段出处与抓取日期；缺失字段显示"官网未提供"；缺失但重要的字段进待确认清单 | P2 |
| 对比 | 多选导师并排比较 | P3 |
| 看板/状态写回 | 随信件延后 | 附录 A |

全站规则：任何字段旁可见 origin 与 confidence，抓取日期必显。

---

## 7. 分期

| 期 | 内容 | 验收 |
| ---- | ---- | ---- |
| P1 | 数据模型 + 爬取框架 + **上交计算机学院**全量打通 | 名单数与官网实际对得上（差异留痕）；重跑零新请求；每个非空字段可追出处 |
| P2 | 上交其余院系 + 静态网页 v1（列表 + 详情） | 全院系名单入库；能按院系/职称/关键词筛；缺失字段与零值字段视觉可区分 |
| P3 | 对比页 + 覆盖率收尾 + issues 清账 | 抽查 20 位：字段出处 100% 可点回原文；issues 队列无未处理项 |
| P4 | 扩面：T1 其余学校，逐校滚动 | 每校：名单全覆盖或失败留痕，无静默丢失 |
| P5+ | 按 T2 → T2b 顺序继续扩面 + 附录 A 延后项按需启动 | 同上 |

硬性顺序两条：爬取框架先于扩面；名单先于富化。P1 先用一个院系把"名单→富化→留痕→网页"整条链跑通，再铺量。

---

## 8. 附录 A：延后项（设计保留，不进本期）

触发条件：导师库主体可用后，按需逐项启动，不并行铺开。

1. **学术数据层**：DBLP（作者消歧、论文年份/会议/合作者）→ OpenAlex（引用数、h-index、主题）→ arXiv/OpenAlex（摘要）。身份链路：DBLP 定身份 → OpenAlex 补影响力 → arXiv/OpenAlex 补摘要。DBLP 需限速重试（实测会 503）；Google Scholar（403）、Semantic Scholar（无 key 即 429）已实测放弃。
2. **AI 分析层**：任务限定为方向标签归一化、方向脉络与漂移、代表作要点、活跃度与署名解读、匹配点、待确认清单。硬约束：输入只限库内已抓材料；结论必带 evidence，无 evidence 丢弃；结果落 `cache/ai`，键 = 输入指纹 + prompt_version + model；改 prompt 必升版本号；信件草稿中匹配不到 evidence 的断言构建失败。
3. **学生画像**：`data/student.yaml`——基本信息、院校背景、成绩排名、科研经历（每条要有能讲出细节的内容，不只是标题）、技能栈、意向方向、时间线约束。
4. **SOP + 信件草稿**：SOP 独立成文，先于信件生成器。信件骨架：主题行 / 一句话自我定位 / 为什么是他（引用具体论文 + 真实疑问或延伸想法）/ 你能贡献什么 / 附件 / 简短收尾与明确的下一步。自检：把人名和论文名换掉还成立的句子是模板废话，删掉。
5. **状态写回 + 看板**：联系状态、发信记录、看板视图；本地小服务提供 PATCH 写回 YAML，不引 Flask/FastAPI。

---

## 9. 已决事项（原未决项，2026-09-03 统一决策）

| 项 | 决策 |
| ---- | ---- |
| JS 渲染站点 | 引入 Playwright 作渲染抓取（已接入 `fetch_rendered`，带缓存/滚动/降级） |
| git 仓库 | ~~暂不初始化~~ 已变更：09-04 初始化并推送 GitHub（见 §10） |
| 数据更新节奏 | 手动重跑（想起来跑 `--refresh`） |
| 失败/空壳校换源 | 不换源，官网现状即入库，缺就缺 |
| 人工信息通道 | 暂不做（手改 YAML 通道天然可用） |
| T3 冻结 | 全部解冻（北邮/西电/国防科大/南科大/上科大），能否入库取决于网络可达性 |
| 对比页 | 库稳定后再做 |
| facet 筛选 | ~~暂不做~~ 已变更：09-04 实现 13 类关键词规则 facet，列表页可多选筛选 |
| 时间线 | 敏捷迭代，逐步完善 |

---

## 10. 进度快照（2026-09-06 晚，详情见 HANDOVER.md）

- 规模：**36 校 42 院系 5649 人**，全量 YAML + 静态站；preflight 绿（0 PROBLEM）/audit 常态化
- 全库覆盖：邮箱 80%（4550）、职称 74%（4212）、简介 62%（3534）、博导硕导 **53%（3025）**（分院系明细见 HANDOVER §3，`audit.py` 可随时复核）
- P1/P2/P4 基本完成；P3 对比页推迟；附录 A（学术数据层/AI 分析/学生画像/SOP/信件/看板）未启动
- 相比 v0.5 决策的变化：facet 筛选已实现（13 类）；T3 全部解冻并接入西电/南科大/上科大；git 已初始化并推送
- 已接入覆盖 T1 全部（清北华五 7 校）、CAS 三所、T2/T2b 多数；09-05 新增北航/天大/北理工/人大信院/湖大/华南理工 + CDP 攻克北邮/重大/电子科大
- **CDP 真实浏览器后端上线**：瑞数/网防 WAF 拦客户端指纹，CDP 连本机真实 Chrome 即放行——此前"无法接入"的北邮（217 人 邮箱 80%）/重大（77 人 博导硕导 100%）/电子科大（189 人 邮箱 97%）全部攻克
- **存量低覆盖专项完成（09-06）**：川大职称 23→71%（tsites bs 信息块扫描）、吉大邮箱 43→74%（**图片邮箱 rapidocr 识别**，52 人逐图目检）、东北邮箱 53→74%（简介正文标签变体）、同济邮箱 58→66%（wp 粘连/吞词两坑修复）；大连理工解密接口复测为服务端挂死（真缺失留痕）。确缺院系均已逐页核验留痕
- **supervisor 全库专项完成（09-06 晚）**：三级提取管线（结构化标签/头部徽标/简介职称相邻自述）落地 `crawler/supervisor_util.py`，全库 40%→53%；**邮箱第三轮**：ruc/ai 67→100%（`(@)` 混淆）、xjtu 68→73%；**职称数据质量专项**：74 条粘连值拆分 + 8 条解析垃圾清空（`scripts/fix_titles.py` 迁移）；脏记录清理（sdu 卡片锚点假人 4 条、ruc/ai"中文"）；issue schema 修复（reviewed 持久化）；phase_roster 加 stable_urls 防 zju URL 漂移重复入库
- 未接入仅剩华科/南开（网络超时，09-06 直连+代理复测仍 TCP 阻断）与国防科大（用户决定不做）；下一步=Phase 3 产品层（状态写回+套磁看板优先）/ verified 抽查，见 HANDOVER §9
